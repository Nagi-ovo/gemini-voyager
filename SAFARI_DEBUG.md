# Safari 扩展调试指南

## 🔍 系统排查清单

### 第一步：确认扩展状态

**Safari → 设置 → 扩展**

检查项：
- [ ] 左侧列表能看到 "Gemini Voyager"
- [ ] 复选框已勾选 ☑️
- [ ] 点击后右侧显示详情
- [ ] 权限设置选择了 "在所有网站上" 或 "询问"

如果显示 "询问" 模式：
- 访问 Gemini 时应该弹出权限请求
- 如果没弹出，改为 "在所有网站上"

---

### 第二步：测试基本加载

**访问：https://gemini.google.com**

1. 完全刷新页面（⌘⇧R）
2. 右键页面 → "检查元素"（Inspect Element）
3. 切换到 "控制台"（Console）标签

**查找以下内容：**

✅ 正常情况应该看到：
```
[FolderManager] Initialized
[TimelineManager] Initialized
```

❌ 如果看到红色错误：
- 截图发给我
- 或者复制错误信息

---

### 第三步：检查 DOM 元素

在 Console 中输入以下命令测试：

```javascript
// 检查扩展是否注入了元素
document.querySelector('.gemini-timeline-bar')
```

**结果判断：**
- 如果返回 `null` → 扩展没有加载
- 如果返回 `<div class="gemini-timeline-bar">` → 扩展已加载但可能隐藏

---

### 第四步：Safari 开发者工具

**菜单栏 → 开发 → Web Extension Background Pages**

应该能看到 "Gemini Voyager Extension"：
- 点击它会打开扩展的后台页面控制台
- 查看是否有错误信息

---

### 第五步：完全重置 Safari

如果上面都不行，尝试：

1. **关闭 Safari**（完全退出，⌘Q）
2. **清除缓存**：
   ```bash
   # 清除 Safari 缓存
   rm -rf ~/Library/Caches/com.apple.Safari
   rm -rf ~/Library/Safari/LocalStorage
   ```
3. **重启 Xcode App**
4. **重新打开 Safari**

---

## 🐛 常见问题

### 问题 1: 扩展列表显示但页面无反应

**可能原因：**
- 权限未授予
- 页面 URL 不匹配（确保是 https://gemini.google.com）
- Safari 缓存了旧版本

**解决：**
1. 检查 URL 是否正确
2. 硬刷新页面（⌘⇧R）
3. 清除 Safari 缓存

### 问题 2: Content Script 加载失败

**检查：**
```bash
# 查看构建的文件是否存在
ls -la dist_safari/assets/ | grep index.tsx-loader
ls -la dist_safari/contentStyle.css
```

**如果文件不存在或损坏：**
```bash
# 重新构建
npm run build:safari
# 重新转换
rm -rf "Gemini Voyager"
xcrun safari-web-extension-converter dist_safari --app-name "Gemini Voyager" --macos-only
```

### 问题 3: Safari 不加载开发中的扩展

**必须启用：**
1. Safari → 设置 → 高级 → ☑️ 显示"开发"菜单
2. 开发 → ☑️ 允许未签名的扩展

---

## 📱 联系信息

如果以上都不行，请提供：
1. Safari 设置 → 扩展 页面的截图
2. Gemini 页面控制台的截图（F12 或右键检查）
3. Xcode 控制台的任何错误信息

