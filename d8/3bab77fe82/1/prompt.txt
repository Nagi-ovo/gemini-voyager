bump

---

Update skills. 如果你发现没有 change 的话，你自动根据 commit 记录和这个实际的代码修改创建一个。

要抓住重点，不要让用户阅读起来很费劲。

---

需要手动开启的功能要注明

---

需要手动开启的功能要注明,并且最好引用对应语言里文档部分对应的文档。然后你可能还得加一部分代码，方便在 ChangeLog Markdown 里面去引用 LitePress 的 Documentation。

这样的话，你只需要 ref 一下这个标题，它就自动引用过去了，并且能够自动去 match 对应的语言。

---

build

---

表格现在会跟随聊天宽度设置自动展开。 这没有对应的文档哦

---

所以为什么会导致这个问题呢？

---

push tag